import { NotFoundStyle } from "./styled";
import { Header } from "../../components/Header";

export const NotFound = () => {
    return (
        <NotFoundStyle>
            <Header/>
            Not found
        </NotFoundStyle>
    );
}