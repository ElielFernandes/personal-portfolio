import styled from "styled-components";

export const AnimationStyle = styled.div`
  
  .content {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    min-height: calc(100vh - 4px);
  }
`;